export type ApiLocality = {
    id: number;
    nombre: string;
    departamento: string;
    activo: boolean;
  };
  
  // El tipo que usaremos en la UI:
  export type Locality = {
    id: number;
    nombre: string;
    departamento: string;
  };